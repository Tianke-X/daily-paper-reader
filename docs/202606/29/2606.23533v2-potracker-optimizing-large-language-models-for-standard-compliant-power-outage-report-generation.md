---
title: "POTracker: Optimizing Large Language Models for Standard-Compliant Power Outage Report Generation"
title_zh: POTracker：优化大型语言模型以生成符合标准的停电报告
authors: "Hung Phan, Aniroop Naladala, Dubey Avanindra, Supryia Chinthavali, Lunga Dalton, Ali Jannesari"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23533v2"
tags: ["query:power-ticket"]
score: 9.0
evidence: 针对电力停电报告生成优化的LLM，符合标准格式
tldr: "电力公司停电报告需符合行业标准格式，但现有大语言模型难以生成严格合规的领域数据。为此提出POTracker，通过新损失函数POTrackerLoss联合优化文本和结构相似性，微调Qwen2.5-7B-Instruct模型。在1000份报告上，准确率提升51%，结构准确率达86.47%，人工评分4.03/5。该方法实现了标准合规的自动化报告生成，显著降低人工编写成本。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23533v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 775, \"height\": 362, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23533v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 782, \"height\": 573, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23533v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1641, \"height\": 561, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23533v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 849, \"height\": 358, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23533v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1585, \"height\": 597, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23533v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 528, \"height\": 285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23533v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 721, \"height\": 406, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23533v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 284, \"height\": 462, \"label\": \"Table\"}]"
motivation: 现有LLM在领域数据生成中难以满足严格格式和标准要求，导致输出不可直接使用。
method: 提出POTracker，微调Qwen2.5-7B-Instruct，使用POTrackerLoss联合优化文本和标签结构相似性。
result: "在1000份报告上准确率提升51%，结构准确率86.47%，人工评分4.03/5。"
conclusion: POTracker有效实现标准合规的停电报告生成，优于现有微调方法和规则方法。
---

## 摘要
最近的大型语言模型（LLMs）擅长通用文本生成，但由于输出必须遵循严格的格式和结构规则，因此仍然难以将其用于特定领域的数据生成。与问答或翻译等开放式任务不同，特定领域的生成必须既语义正确，又符合现有指南和标准。在这项工作中，我们研究了美国电力停电报告的全国互操作性问题。在实践中，停电报告需要是机器可读的（例如JSON或XML），并且必须严格遵循能源部门监管机构的要求。为了解决这个问题，我们提出了POTracker，一个用于停电报告生成的优化LLM。我们使用我们提出的目标对Qwen2.5-7B-Instruct进行微调。关键贡献是一个新的损失函数POTrackerLoss，它考虑了生成报告与真实报告之间的文本相似性和结构（标签）相似性。我们在一个包含1000份停电报告的数据集上评估了POTracker，并将其与五种著名的微调方法和一种基于规则的XML转换方法进行了比较。结果表明，POTracker优于其他微调方法，总体准确率提高了高达51%，生成的停电报告的结构准确率达到86.47%。此外，我们进行了一项人工研究来评估真实标准报告的质量，领域专家对生成的标签在0-5分制上给出了平均4.03分。

## Abstract
Recent large language models (LLMs) are good at general text generation, but it is still hard to use them for domain-specific data generation because the output must follow strict formatting and structural rules. Unlike open-ended tasks such as question answering or translation, domain-specific generation must be both semantically correct and compliant with existing guidelines and standards. In this work, we study the nationwide interoperability problem of utility power outage reports in the United States. In practice, outage reports need to be machine-readable (e.g., JSON or XML) and must strictly follow requirements from energy-sector regulatory bodies. To address this problem, we propose POTracker, an optimized LLM for power outage report generation. We fine-tune Qwen2.5-7B-Instruct using our proposed objective. The key contribution is a new loss function, POTrackerLoss, that considers both textual similarity and structural (tag) similarity between the generated report and the ground-truth report. We evaluate POTracker on a dataset of 1,000 power outage reports and compare it with five well-known fine-tuning methods and one rule-based XML conversion method. Results show that POTracker outperforms other fine-tuning approaches, improving overall accuracy by up to 51% and reaching 86.47% structural accuracy for generated power outage reports. In addition, we conduct a human study to assess the quality of the ground-truth standard reports, where domain experts assign the generated labels an average score of 4.03 on a 0--5 scale.

---

## 论文详细总结（自动生成）

# POTracker 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：美国电力公司提交的停电报告（Power Outage Reports）格式多样、结构混乱（如非标准XML、JSON、自由文本），但必须转换为符合行业标准（CIM-IEC 61968-3）和ODIN（Outage Data Initiative Nationwide）规范的机器可读格式（XML/JSON）。现有大语言模型（LLMs）擅长通用文本生成，却难以保证严格的结构合规性和语义准确性。
- **背景**：ORNL（Oak Ridge National Laboratory）的ODIN系统要求停电报告遵循统一数字标准，以便跨机构互操作。人工转换效率低、成本高，需要自动化方案。
- **整体含义**：提出POTracker框架，通过优化LLM（Qwen2.5-7B-Instruct）的微调过程，使其能够从非标准输入生成符合行业标准的停电报告，兼顾文本语义（内容）和标签结构（格式）的正确性，解决了领域特定结构化数据生成的痛点。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 提出一种新的损失函数 **POTracker Loss**，在传统交叉熵损失的基础上，加入序列级别的质量信号，联合优化文本相似性和XML标签结构相似性。
- 微调开源LLM（Qwen2.5-7B-Instruct），使其学习从非标准报告到标准报告的映射。

### 关键技术细节
- **数据预处理**：从ODIN系统收集非标准报告，提取关键元数据（位置、时间、受影响用户数等）。
- **数据标签生成**：使用闭源LLM GPT-5.2（通过精心设计的提示）将非标准报告转换为符合CIM-IEC-61968-3的标准报告，作为训练标签。
- **POTracker Metric（评估指标）**：综合文本相似度（余弦相似度）和标签结构相似度（difflib.SequenceMatcher），公式为：
  \[
  \text{POTracker\_Metric} = \alpha \cdot \text{Text\_Similarity} + (1-\alpha) \cdot \text{Tag\_Similarity}
  \]
  其中 \(\alpha\) 默认0.5。
- **POTracker Loss（算法1）**：
  1. 计算标准 token 级交叉熵损失 \(L_{\text{CE}}\)；
  2. 对当前批次的 logits 贪心解码生成预测XML，并与真实XML计算相似度得分 \(s\)；
  3. 转换为惩罚项 \(p = 1 - s\)；
  4. 总损失：\(L_{\text{total}} = L_{\text{CE}} + \lambda \cdot p\)，\(\lambda\) 默认0.5。
- **对比的微调基线方法**：
  - **Token Weight Cross Entropy (TW-CE)**：对XML标签相关token赋予更高权重。
  - **Tag Masking Cross Entropy (TM-CE)**：对标签token使用 mask，提高其贡献。
  - **Example Weight Cross Entropy with PO Custom Loss (EW-CE-PO)**：基于样本级POTracker Metric对样本加权。
  - **Direct Preference Optimization (DPO)**：通过构造坏样本（随机破坏XML结构）进行偏好优化。
- **规则基线**：由两位领域专家人工编写转换规则（如将非标准标签映射到标准标签）。

## 3. 实验设计

### 数据集
- 收集2024年1月至2025年3月的美国停电报告，共1000条。
- 划分为：800条训练，100条验证，100条测试。
- 标签由GPT-5.2生成，经人工验证（评分4.03/5），保证质量。

### Benchmark
- 对比5种微调方法（TW-CE、TM-CE、EW-CE-PO、DPO、原始Qwen2.5-7B-Instruct）和1种规则方法（Rule-based Conversion）。

### 评估指标
- 使用POTracker Metric的三种变体：
  - Overall Accuracy（\(\alpha=0.5\)）
  - Text Accuracy（\(\alpha=1.0\)，仅文本相似度）
  - Tag Accuracy（\(\alpha=0.0\)，仅标签结构相似度）

### 实验内容
- 主要性能对比（表1）。
- 人类评估标签质量（50样本，两位专家，0-5评分）。
- 改变POTracker Loss权重\(\alpha\)的消融实验（表5，\(\alpha\)从0.1到1.0）。

## 4. 资源与算力

论文明确说明：
- **硬件**：单节点服务器，配备1×NVIDIA A100 GPU（40 GB VRAM），Intel Core i9 CPU（16核），100 GB RAM，1 TB NVMe SSD。
- **软件**：Ubuntu 24.04，CUDA 12.0。
- **训练时长**：未明确给出训练轮数或时间，仅描述为“all training and evaluation on a dedicated single-node server”。
- **备注**：算力资源较主流，但未提及其他基础设施（如分布式训练），实验规模适中。

## 5. 实验数量与充分性

- **主要实验**：对比了6种方法（5种微调+1种规则），在100条测试集上报告结果，使用2个指标变体（文本和结构）。
- **消融实验**：对POTracker Loss中的权重\(\alpha\)从0.1到1.0进行了10个取值测试，观察整体准确率变化。
- **人类评估**：50个样本的双专家打分（平均4.03/5），验证标签质量。
- **充分性评价**：
  - 实验覆盖了主流的微调策略（加权、掩码、示例加权、偏好优化）和规则方法，对比全面。
  - 单一测试集（100条），样本量偏小，可能不足以代表真实分布，但领域特定数据收集困难可以理解。
  - 消融实验仅针对一个超参数，缺少对\(\lambda\)、模型规模等的进一步分析。
  - 缺乏与其他闭源模型（如GPT-4）的直接比较（仅用于标签生成）。
  - 未评估不同输入格式（如纯文本、错误XML）的鲁棒性。
- **公平性**：所有方法使用相同的基础模型（Qwen2.5-7B-Instruct），提示统一（不含in-context示例），训练集相同。但DPO的负样本构造方式可能影响结果，论文已指出其局限。

## 6. 主要结论与发现

- **原始LLM效果差**：Qwen2.5-7B-Instruct直接生成标准报告，整体准确率仅16.20%，标签准确率3.56%。
- **交叉熵变体无效**：TW-CE、TM-CE、EW-CE-PO均未带来显著提升（整体约16%），表明标准NLP损失不适合结构化生成。
- **DPO收效甚微**：整体16.16%，Tag准确率3.43%，负样本质量不足是可能原因。
- **规则方法表现优异**：规则方法整体61.46%，Tag准确率79.00%，远超LLM微调方法，说明确定性规则在结构约束上具有天然优势。
- **POTracker Loss显著提升**：整体准确率69.14%（提升约53%），Text 51.81%，Tag 86.47%，在格式合规性和语义准确性上均达到最优（相比基线）。
- **人类评分表明标签质量高**：GPT-5.2生成的标准报告平均得分4.03/5，中位数5，具备指导微调的可靠性。

## 7. 优点

- **针对性强的损失函数**：POTracker Loss将结构相似性作为序列级惩罚，直接优化生成XML的合规性，突破了交叉熵只关注token级似然的局限。
- **综合评估指标**：POTracker Metric同时考虑文本和标签结构，更贴合领域需求。
- **系统完整**：从数据预处理、标签生成（利用闭源模型）、微调到评估，形成闭环。
- **可复现性**：使用开源模型Qwen2.5-7B-Instruct，公开代码（链接），硬件配置透明。
- **人工验证标签**：确保训练数据质量，降低噪声风险。

## 8. 不足与局限

- **领域单一**：只针对电力停电报告，未验证在其他结构化生成任务（如天气预报、金融报告）的有效性。
- **依赖闭源模型生成标签**：GPT-5.2虽经人工验证，但仍可能引入偏差或格式瑕疵，且无法完全替代真实人类标注。
- **测试集规模小**：仅100条，统计显著性有限，未见置信区间或多次重复实验。
- **消融实验不全面**：仅变化\(\alpha\)，未深入分析\(\lambda\)、模型不同层、是否使用LoRA等参数高效微调的影响。
- **未与其他大型开源LLM比较**：如Llama3、Mistral等，仅对比了Qwen的一个尺寸（7B），缺乏可扩展性分析。
- **规则方法作为基线但需大量人工**：规则编写依赖两位专家，成本高且难以推广到其他格式/领域。
- **DPO的负样本构造简单**：随机破坏缺乏语义针对性，导致偏好信号弱。需探索更精细的对抗负样本生成。
- **训练细节不完整**：未报告学习率、批大小、训练步数，难以复现和公平比较。
- **未见消融POTracker Loss中两大项影响**：仅使用交叉熵vs仅使用惩罚项的效果未单独比较。

（完）
