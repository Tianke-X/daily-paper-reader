---
title: Latent Reasoning with Normalizing Flows
title_zh: 基于归一化流的潜在推理
authors: "Guancheng Tu, Xiangjun Fu, Suhao Yu, Yao Tang, Haoqiang Kang, Lianhui Qin, Yizhe Zhang, Jiatao Gu"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06447v1"
tags: ["query:pwt"]
score: 8.0
evidence: 潜推理替代链式思维提升推理能力
tldr: 大语言模型依赖显式思维链推理，但串行离散生成效率低。本文提出NF-CoT，在LLM骨干中嵌入TARFlow风格归一化流，对从显式CoT蒸馏的连续思维建立可处理概率模型，同时用标准LM头生成文本。在代码生成基准上，NF-CoT相比显式CoT和先前潜在推理基线提高通过率，并显著降低中间推理成本。该方法保留了从左到右生成、KV缓存、概率采样等优势，为潜在推理提供精确似然，支持策略梯度优化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06447v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 768, \"height\": 676, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06447v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1702, \"height\": 782, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06447v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1633, \"height\": 568, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06447v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1633, \"height\": 1014, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06447v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1720, \"height\": 421, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06447v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1687, \"height\": 778, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06447v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1720, \"height\": 997, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06447v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1345, \"height\": 202, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06447v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 991, \"height\": 201, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06447v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 727, \"height\": 204, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06447v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 849, \"height\": 420, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06447v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1694, \"height\": 746, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06447v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1719, \"height\": 285, \"label\": \"Table\"}]"
motivation: 显式CoT推理需逐词生成，延迟高；现有潜在推理方法牺牲了自回归LM的从左到右生成、KV缓存等关键优势。
method: 提出NF-CoT，在LLM中插入归一化流层对连续思维建模，与文本生成共享因果流，实现精确似然估计与KV缓存兼容的解码。
result: "在代码生成基准测试中，NF-CoT的pass@k指标超过显式CoT和潜在推理基线，同时中间推理Token数大幅减少。"
conclusion: NF-CoT融合了连续潜在推理的高效性与CoT的自回归优势，为语言模型推理提供了一种可扩展的新范式。
---

## 摘要
大型语言模型通常通过生成显式的思维链（CoT）来改进推理，这显示了中间计算的重要性。然而，文本思维链迫使这种计算通过离散、串行且面向通信的令牌流进行：每一步推理都必须在模型继续之前被语言化，即使底层更新是语义性的、不确定的或仅部分形成的。潜在推理通过在提交到文本之前以紧凑连续状态执行中间计算，提供了一种更高带宽的替代方案。然而，现有的潜在推理方法往往牺牲了使CoT在自回归语言模型中有效的关键优势，包括原生从左到右生成、概率采样、与KV缓存解码的兼容性以及可处理的似然估计。我们提出NF-CoT，一种潜在推理框架，通过使用归一化流建模连续思想来保留这些优势。NF-CoT在LLM骨干内部实例化了一个TARFlow风格的归一化流，为从显式CoT中提炼的紧凑连续思想定义了一个可处理的概率模型。连续思想位置由NF头生成，而文本位置由同一因果流中的标准LM头生成。这种设计为潜在思想提供了精确的似然，使概率性的从左到右解码能够使用原始KV缓存，并支持在潜在推理空间中进行直接策略梯度优化。在代码生成基准测试中，NF-CoT比显式CoT和先前的潜在推理基线提高了通过率，同时显著降低了中间推理成本。

## Abstract
Large language models often improve reasoning by generating explicit chain-of-thought (CoT), demonstrating the importance of intermediate computation. However, textual CoT forces this computation through a discrete, serial, and communication-oriented token stream: each reasoning step must be verbalized before the model can proceed, even when the underlying update is semantic, uncertain, or only partially formed. Latent reasoning offers a higher-bandwidth alternative by performing intermediate computation in compact continuous states before committing to text. Yet existing latent-reasoning methods often sacrifice key advantages that make CoT effective in autoregressive language models, including native left-to-right generation, probabilistic sampling, compatibility with KV-cache decoding, and tractable likelihood estimation. We propose NF-CoT, a latent reasoning framework that preserves these advantages by modeling continuous thoughts with normalizing flows. NF-CoT instantiates a TARFlow-style normalizing flow inside the LLM backbone, defining a tractable probability model over compact continuous thoughts distilled from explicit CoT. Continuous-thought positions are generated by an NF head, while text positions are generated by the standard LM head within the same causal stream. This design provides exact likelihoods for latent thoughts, enables probabilistic left-to-right decoding with the original KV cache, and supports direct policy-gradient optimization in the latent reasoning space. On code-generation benchmarks, NF-CoT improves pass rates over explicit-CoT and prior latent-reasoning baselines while substantially reducing intermediate-reasoning cost.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：大型语言模型（LLM）依赖显式思维链（CoT）进行推理，通过生成中间文本步骤来提升性能。但文本CoT是离散、串行、低信息密度的通信流，导致推理成本高、延迟大。现有潜在推理方法（如Coconut、LaDiR）试图用连续状态替代文本，但牺牲了原生自回归LM的关键优势，包括：
  - 从左到右生成
  - 概率采样
  - KV缓存兼容解码
  - 可处理的似然估计（用于评分和RL）
- **动机**：需要一种既能利用连续潜在空间的高带宽，又能保留显式CoT所有建模接口（采样、评分、解码）的框架。

## 2. 方法论：核心思想、关键技术细节

### 2.1 核心思想
- **NF-CoT**：在LLM骨干内嵌入一个TARFlow风格的归一化流（normalizing flow），将显式CoT提炼为紧凑连续思想（continuous thoughts），并在统一因果流中联合建模潜在推理和文本答案。

### 2.2 关键技术细节
1. **两阶段连续表示**：
   - 使用预训练的VAE将显式CoT文本 \( d_{1:L} \) 编码为连续目标表示 \( e_{1:K} \)（固定长度 K=64）。
   - 通过可逆的**浅层流块**（5个MetaBlocks，交替Identity/Flip排列）将 \( e \) 映射到**LLM面对的思想空间** \( u_{1:K} \)（信息等价，可逆变换）。Jacobian三角矩阵，log-determinant可计算。

2. **统一因果建模**：
   - 在LLM骨干的因果流中，连续思想位置由**NF头**输出高斯条件密度参数 \( \mu_\theta, \sigma_\theta \)；文本答案位置由**标准LM头**输出token logits。两者共享同一骨干和KV缓存。
   - 概率模型（式3.1）：\( p_\theta(u_{1:K}|q) = \prod_{i=1}^K \mathcal{N}(u_i; \mu_\theta(q,u_{<i}), \text{diag}(\sigma_\theta^2(q,u_{<i}))) \)。

3. **训练目标**（式3.3）：
   - 流损失：\( \mathcal{L}_{\text{flow}} = -\log p_\theta(e_{1:K}|q) \)（通过式3.2计算）。
   - 文本损失：\( \mathcal{L}_{\text{text}} = -\sum \log p_\theta(x_j|q, u_{1:K}, x_{<j}) \)。
   - 最终：\( \mathcal{L}_{\text{sup}} = \lambda_{\text{flow}} \mathcal{L}_{\text{flow}} + \lambda_{\text{text}} \mathcal{L}_{\text{text}} \)。

4. **训练课程**：
   - **Stage 1**：冻结LLM骨干，只训练浅层流块和投影层（100,000样本，1 epoch）。
   - **Stage 2**：解冻所有参数，联合端到端训练（全数据集，2 epoch）。

5. **推理**：
   - 直接采样 \( u \) 空间（跳过浅层流块）：自回归从左到右从式3.1采样连续思想，然后切换到LM头解码文本。KV缓存完全复用。

6. **强化学习扩展**：
   - 冻结浅层流块，使用GRPO对潜在思想轨迹进行策略梯度优化（式3.4-3.5）。奖励来自单元测试反馈。

## 3. 实验设计

### 3.1 数据集
- **微调**：LingCoder（140万Python指令样本）。
- **RL**：额外使用AceCoder和KodCode混合数据（20K问题，150步训练）。

### 3.2 基准（Benchmark）
- 代码生成：HumanEval、HumanEval+、MBPP、MBPP+、LiveCodeBench v6。
- 指标：pass@1（16样本平均），pass@k（k=2~128）。

### 3.3 对比方法
- **开源模型**：Qwen2.5-Coder、OpenCoder、OlympicCoder、Seed-Coder。
- **扩散模型**：Dream、LLaDA、Diffu-Coder、Dream-Coder、d1。
- **循环潜在推理**：Ouro。
- **相同骨干（Qwen3-8B-Base）基线**：Standard SFT、Soft Thinking、TaH+、LaVAE（VAE替代流）、LaDiR（扩散替代流）。
- 报告两个变体：NF-CoT (Dual-Path) 和 NF-CoT (Unified)。

## 4. 资源与算力

- **硬件**：NVIDIA DGX B200节点。
  - Stage 1：4 GPU。
  - Stage 2：8 GPU。
- **训练配置**：
  - 混合精度bfloat16，FSDP混合分片，激活检查点。
  - Stage 1：global batch 32，学习率1e-4，100步热身。
  - Stage 2：global batch 64，学习率5e-5，200步热身。
- **RL**：150步，batch size 64 prompts，每个prompt 8条轨迹。
- **未明确总训练时长（小时数）**，但提供了吞吐量数据（表3）。

## 5. 实验数量与充分性

- **主要实验**：在5个代码基准测试上报告pass@1（表1），包含11个对比方法+5个相同骨干变体。
- **扩展实验**：
  - pass@k缩放分析（图3）：k=1~128。
  - RL效果：pass@1提升（表1）和pass@k多样性（图4）。
  - 推理效率对比（表2）和训练吞吐量（表3）。
  - 消融研究：训练课程（表4）、Unified vs Dual-Path（表1间接体现）。
  - 潜在空间扰动鲁棒性（图5）。
  - 输出结构化多样性分析（附录图C.2）。
- **充分性**：实验覆盖全面，对比公平（使用同一骨干Qwen3-8B-Base），提供统计置信区间，消融和鲁棒性分析增强了结论可信度。

## 6. 主要结论与发现

1. **性能提升**：NF-CoT (Unified) 在5个基准上平均pass@1达到68.8，比Qwen3-8B-Base提高13.0%，比最强潜在推理基线LaDiR高7.1%，并超越所有开源模型（如OlympicCoder）。
2. **效率优势**：相比LaDiR，NF-CoT (Unified) 推理速度提升2.70倍（总时间从625.3s降至325.6s），每样本FLOPs降低2.48倍；训练吞吐量提升2.85倍。
3. **多样性保持**：pass@k缩放显示NF-CoT随采样数增加持续改善，不饱和；RL后仍保持多样性（图4）。
4. **鲁棒性**：潜在空间平滑，加噪扰动不影响pass@1，但改变生成代码的表面形式（图5）。
5. **RL兼容**：策略梯度优化可直接作用于连续思想，提升pass@1而不破坏分布多样性。

## 7. 优点

- **方法创新**：首次将可逆归一化流与LLM骨干结合，同时获得连续表示、精确似然、自回归生成、KV缓存复用——保留了显式CoT的全部接口优势。
- **架构优雅**：统一因果设计（Unified）消除了潜在表示与文本解码之间的接口不匹配，训练和推理更加简洁高效。
- **效率突出**：相比扩散方法（LaDiR），避免了多步去噪迭代，实现显著提速和降本。
- **RL兼容性好**：精确似然使得在潜在空间进行策略梯度优化成为可能，且不牺牲多样性。
- **实验全面**：覆盖多个基准、多种对比方法、消融和鲁棒性分析，结论扎实。

## 8. 不足与局限

- **任务单一性**：仅验证代码生成任务，未扩展到其他推理领域（如数学、常识推理），泛化性未知。
- **对显式CoT的依赖性**：训练数据来自显式CoT的VAE编码，因此潜在表示继承其偏差；固定潜在长度（64）可能不适用于所有问题。
- **RL奖励限制**：强化学习依赖单元测试奖励，仅适用于代码等可自动验证的领域；对于无验证器的通用推理任务难以直接使用。
- **安全性风险**：改进的代码生成仍可能产生看似正确但包含安全漏洞的代码；连续潜在空间不可读，难以解释内部推理过程（解码CoT仅为定性探针）。
- **未见速度与扩展性细节**：未报告大规模模型（如70B）上的表现，且总训练时长未明确给出。

（完）
