---
title: "Nous: A Predictive World Model for Long-Term Agent Memory"
title_zh: Nous：面向长期智能体记忆的预测世界模型
authors: Pranav Singh
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.22030v1"
tags: ["query:pwt"]
score: 9.0
evidence: 无需训练的预测世界模型用于长期智能体记忆
tldr: 长期智能体记忆面临存储效率与预测能力不足的问题。Nous提出基于预测世界模型的架构，为每个实体-属性对维护概率分布，通过信息惊喜度评分更新，仅存储先验到后验的转移记录。在LoCoMo基准上，以GPT-4o-mini为骨干，单跳F1达63.50，多跳55.32，时间58.57，开放域62.50，超越A-MEM和BeliefMem。该方法无需外部向量数据库，自然实现遗忘与身份解析。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22030v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1702, \"height\": 614, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22030v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 856, \"height\": 349, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22030v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 713, \"height\": 401, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22030v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 734, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22030v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 783, \"height\": 328, \"label\": \"Table\"}]"
motivation: 传统记忆存储事实或向量，缺乏预测性与效率，Nous基于知识即预测原则设计新型记忆架构。
method: 每个实体-属性对维护概率分布，计算观察的惊喜度，通过贝叶斯后验更新，存储转移记录而非事实，遗忘由熵衰减驱动。
result: "在LoCoMo基准（1,540题）上，单跳F1 63.50，多跳55.32，时间58.57，开放域62.50，优于A-MEM与BeliefMem。"
conclusion: 预测世界模型有效实现智能体长期记忆，无需外部存储，为记忆架构提供新方向。
---

## 摘要
我们提出了Nous，一种新颖的智能体记忆架构，其根本原则是知识即预测，而非存储。Nous不将事实持久化为数据库记录、向量嵌入或知识图谱三元组，而是维护一个预测世界模型：一组称为维度的分类概率分布，每个维度对应对话中观察到的实体-属性对。每个新观察依据其信息论惊喜度S = -log2 P(obs | D)进行评分，并通过封闭形式的贝叶斯后验更新分布。主要存储产物是增量，即从先验信念到后验信念的转变记录，而非事实本身。遗忘自然发生为向均匀分布的熵衰减，身份解析通过实体维度集之间的互信息来处理。在LoCoMo长期对话记忆基准上，以GPT-4o-mini为骨干，对十段对话（1540个问题）进行评估，Nous在单跳、多跳、时间和开放域上分别取得了63.50、55.32、58.57和62.50的F1值。与A-MEM自报的GPT-4o-mini结果相比，Nous在四个类别中的三个上取得了显著提升，但我们注意到各独立引用的A-MEM结果在类别分配上存在分歧，我们公开讨论这一可重复性问题，而非单方面解决。此外，我们与BeliefMem进行了比较，后者是一个同期开发的系统，基于同样的核心前提，即基于信念而非确定性的记忆；在相同的基准和骨干下，Nous自报的所有四个类别数值均超过BeliefMem自报数值，但我们指出两个评估流程之间存在若干未受控差异，因此这并非完全受控的比较。Nous无需外部向量数据库或图引擎。

## Abstract
We present Nous, a novel agent memory architecture grounded in the principle that knowledge is prediction, not storage. Rather than persisting facts as database records, vector embeddings, or knowledge-graph triples, Nous maintains a predictive world model: a collection of categorical probability distributions, called dimensions, one per entity-attribute pair observed in conversation. Each incoming observation is scored by its information-theoretic surprise S = -log2 P(obs | D), and the distribution is updated via a closed-form Bayesian posterior. The primary stored artifact is the delta, a record of the shift from prior to posterior belief, rather than the fact itself. Forgetting emerges naturally as entropy decay toward the uniform distribution, and identity resolution is handled through mutual information between entity dimension sets. Evaluated on the LoCoMo long-term conversational memory benchmark across ten conversations (1,540 questions) using GPT-4o-mini as backbone, Nous achieves F1 of 63.50 (single-hop), 55.32 (multi-hop), 58.57 (temporal), and 62.50 (open-domain). Against A-MEM's self-reported GPT-4o-mini numbers, Nous shows substantial gains in three of four categories, though we note that independent citations of A-MEM's results disagree with each other on category assignment, a reproducibility issue we discuss openly rather than resolve unilaterally. We additionally compare against BeliefMem, a concurrently developed system built on the same core premise of belief-based rather than deterministic memory; on the same benchmark and backbone, Nous's self-reported numbers exceed BeliefMem's self-reported numbers on all four categories, though we flag several uncontrolled differences between the two evaluation pipelines that prevent this from being a fully controlled comparison. Nous requires no external vector database or graph engine.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：LLM智能体缺乏持续演化的长期记忆。现有方法（RAG、知识图谱、MemGPT、A-MEM等）都将记忆视为事实的数据库，面临三个结构性失败模式：  
  - **冲突无感知**：用户改变信息后，矛盾记录堆积无解决机制。  
  - **遗忘规则粗糙**：基于时间而非语义的删除。  
  - **检索粒度不匹配**：需要聚合多段对话的信息时，事实存储返回单个记录而无累积机制。
- **整体含义**：论文认为记忆应是世界的预测模型（而非存储），借鉴预测编码和自由能原理，提出**基于概率分布的预测世界模型**，将每个实体-属性对建模为概率分布，通过信息论惊喜度和贝叶斯更新来学习和遗忘。目标是实现无外部数据库、自然处理冲突和遗忘的长期记忆系统。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- **知识即预测**：每个实体-属性对维护一个分类概率分布（称为**维度**）。  
- **存储的是变化而非事实**：主要存储制品是**增量**（prior→posterior的转移记录），而非观察到的具体事实。  
- **遗忘是向均匀分布的熵衰减**。  
- **身份解析通过互信息**：比较不同实体共享维度的KL散度。

### 关键技术细节
1. **维度（Dimension）**：  
   - 定义：\(D_{e,a} = (V, p)\)，其中\(V\)是有限词汇，\(p: V \to [0,1]\)是归一化概率，模式值\(\hat{v}\)为当前最佳信念。

2. **信息论惊喜度**：  
   - 公式：\(S(\text{obs}|D_{e,a}) = -\log_2 p(\text{obs})\) bits。  
   - 若obs不在词汇表中，扩展词汇表并设置小概率ε，产生接近最大惊喜。

3. **贝叶斯更新**：  
   - 似然函数：（公式2）  
     \[
     L(\text{obs}|v) = \begin{cases} 1-\epsilon & v=\text{obs} \\ \frac{\epsilon}{|V|-1} & \text{otherwise} \end{cases}
     \]  
   - 后验：\(p_{\text{post}}(v) = \frac{L(\text{obs}|v) p_{\text{prior}}(v)}{\sum_{v'} L(\text{obs}|v') p_{\text{prior}}(v')}\)  
   - 时间复杂度O(|V|)，无需梯度计算。  
   - 矛盾证据自然转移概率质量，无需显式冲突检测。

4. **熵衰减（遗忘）**：  
   - 随时间推移，分布向均匀分布混合：\(p_t(v) = \lambda^{\Delta t} p_0(v) + (1-\lambda^{\Delta t}) U(v)\)，其中\(U(v)=1/|V|\)。  
   - 保证长期未更新的维度趋向完全不确定性。

5. **身份解析**：  
   - 使用两个实体共享属性的对称KL散度之和进行合并决策。

6. **多值聚合（Observed-Values Aggregation）**：  
   - 针对多跳问题需要返回所有历史值，使用阈值θ_min筛选所有惊喜度大于阈值的历史观察值，拼接到上下文。这是对单模态分类分布局限性的补救措施（作者承认这一设计不够原生）。

7. **流水线**：  
   - **摄入阶段**：LLM抽取三元组(e,a,v)，计算惊喜，贝叶斯更新，生成并存储增量到日志。  
   - **查询阶段**：实体BFS（广度优先搜索）最多两跳，使用BM25检索相关增量，组合上下文传递给LLM回答。

## 3. 实验设计：数据集、基准、对比方法

- **基准**：LoCoMo长期对话记忆基准，包含10段双人对话（每段19-32个会话），共1,540个问题，分为单跳（841）、多跳（282）、时间（321）、开放域（96）四类。排除对抗类别（遵循先前协议）。
- **骨干模型**：GPT-4o-mini（用于抽取、回答、判分）。
- **对比方法**：
  - **无记忆基线**（直接QA，无检索上下文）。
  - **A-MEM**（自报GPT-4o-mini结果，但论文指出不同来源对类别分配有矛盾，作者按他们认为正确的分配报告）。
  - **BeliefMem**（同期工作，也基于信念记忆，自报GPT-4o-mini结果）。  
  - 没有直接运行A-MEM或BeliefMem，仅引用其自报结果。
  - 与Zep、Mem0不进行数值对比（因其使用不同骨干和宽松判分协议）。
- **评估指标**：
  - 主要：token-level F1和BLEU-1（严格表面形式，不奖励语义正确但措辞不同的答案）。
  - 辅助：上下文召回率（检索质量），以及GPT-4o-mini判分的四类失败桶（正确/回答缺失/未知/检索缺失）。

## 4. 资源与算力

论文**未明确说明**所使用的GPU型号、数量或训练时长。仅提及使用GPT-4o-mini作为骨干（通过API调用），未提及本地算力消耗。也没有报告训练时间或推理速度数据。

## 5. 实验数量与充分性

- **实验数量**：仅报告了在LoCoMo上的一组完整结果（1,540问题），包含四个类别的F1、BLEU-1、上下文召回率和失败桶分析。  
- **消融实验**：**缺乏**。作者承认没有进行受控消融来隔离各组件贡献（如贝叶斯更新 vs 平面存储、熵衰减 vs 无遗忘、增量聚合 vs 当前状态检索）。  
- **公平性讨论**：  
  - 与A-MEM对比时，指出文献中类别分配矛盾，作者自行决定分配，可能引入不一致。  
  - 与BeliefMem对比时，强调两系统抽取提示、检索方法不同，数值差距可能反映流程差异而非核心表示差异。BeliefMem提供了自己的消融（移除合并操作后F1从42.38降至20.38），但Nous未做类似消融。  
  - 与Zep/Mem0对比被明确说明为“非受控”，仅供方向参考。  
- **充分性评估**：不够充分。缺少消融、跨骨干泛化、多基准验证。作者也承认这是首次公开报告，并计划后续补充。

## 6. 论文的主要结论与发现

- **主要结论**：基于预测世界模型的记忆架构（Nous）在LoCoMo上**优于现有方法**（A-MEM和BeliefMem），尤其是在多跳和开放域上提升显著（多跳F1 55.32 vs A-MEM 27.02；开放域F1 62.50 vs A-MEM 12.14）。  
- **发现**：  
  - 单跳上下文召回率高达86.07%，但F1为63.50，表明**回答提取**是瓶颈而非检索。  
  - 时间问题上下文召回率最低（61.82%），因为时间戳未作为专用维度属性提取。  
  - 开放域检索缺失率最高（37%），因为个性和习惯问题不易映射到实体-属性维度。  
  - 多跳F1与BLEU-1差距大（55.32 vs 23.90），原因是数据聚合机制产生了额外正确项，提高了召回但降低了精确率。  
- **信念表示的价值**：通过与BeliefMem的对比（及其消融结果），共享的核心思想（从证据更新信念而非覆盖）是有效的。

## 7. 优点：方法或实验设计上的亮点

- **方法创新性**：  
  - 首次将“知识即预测”的认知科学原则系统应用于智能体记忆，统一处理冲突解决、遗忘、身份解析、多值聚合。  
  - 计算简洁：贝叶斯后验封闭形式O(|V|)，无梯度，无需外部数据库。  
  - **惊喜度**作为一级日志信号，使每次更新可审计。  
  - 遗忘机制（熵衰减）天然符合不确定性增长，优于启发式时间删除。  
- **实验设计亮点**：  
  - 使用严格token F1（保守指标），避免LLM判分协议的不可靠性。  
  - 引入上下文召回率作为检索诊断，分离检索与回答错误。  
  - 失败桶分析提供细粒度故障定位。  
  - 主动讨论基准文献中的可重复性问题（A-MEM类别分配矛盾），体现学术诚实。  
  - 开源实现，促进可重复性。

## 8. 不足与局限

### 实验覆盖不足
- **缺少消融实验**：无法确定各组件（贝叶斯更新、熵衰减、增量聚合）的独立贡献。
- **仅一个基准**：LoCoMo存在已审计出的标签错误和判分校准问题；未使用LongMemEval（更困难、更细致审计的基准）。
- **仅一种骨干模型**：GPT-4o-mini，未验证对其他LLM的泛化性。

### 架构局限
- **单模态分类分布**：多值属性需要后处理机制（observed-values aggregation），作者承认不原生，BeliefMem的独立伯努利分布对此更优。
- **时间信息嵌入局限**：时间戳仅藏在证据文本中，未作为显式维度属性，导致时间问题上下文召回率最低（61.82%）。
- **检索半径限制**：仅两跳BFS，无法处理需要更远实体链接的多跳问题（9%的检索缺失）。

### 比较方法公平性
- **未自行运行基线**：所有对比基于其他论文的自报数值，可能受不同提示、预处理、评估代码影响。
- **A-MEM类别分配矛盾**：无法确认哪个数值对应哪类，比较有不确定性。
- **与BeliefMem的差异**：作者承认可能存在检索和提示差异导致结果差距，而非核心表示差异。作者计划后续进行匹配流水线的控制实验。

### 其他
- 未报告运行时间、内存消耗等效率指标。
- 未讨论扩展性（维度数量增长时的性能）。
- 未讨论安全性与偏见。

（完）
