---
title: "Measuring Graph-to-Graph Semantic Similarity in Knowledge Graphs: An Empirical Evaluation of Knowledge Graph Embeddings"
title_zh: 知识图谱中图到图语义相似性的度量：知识图谱嵌入的实证评估
authors: "Seungryeol Baek, Wooseok Sim, Hogun Park"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29180v2"
tags: ["query:power-ticket"]
score: 9.0
evidence: 知识图谱嵌入评估
tldr: 知识图谱（KG）中的图级语义相似度评估面临挑战，现有方法多聚焦实体或结构，忽略语义对应。本文构建语义匹配数据集，基于KG嵌入提出EmbPairSim和AvgEmbSim两种评分函数。实验表明EmbPairSim在MRR上比Sentence-BERT高5.3个百分点，参数更少。该方法证明KG嵌入可作为图级语义相似度的紧凑有效信号。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29180v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 877, \"height\": 630, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29180v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1846, \"height\": 554, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29180v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1815, \"height\": 1082, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29180v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 881, \"height\": 312, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29180v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 880, \"height\": 312, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29180v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 881, \"height\": 529, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29180v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 880, \"height\": 686, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29180v2/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 484, \"height\": 276, \"label\": \"Table\"}]"
motivation: 现有KG嵌入方法主要处理实体和三元组，缺乏图级语义，而结构相似不能保证语义对应。
method: 构建由文本修改生成的KG对数据集，提出最大成对实体相似度（EmbPairSim）和频率加权质心（AvgEmbSim）两种评分函数。
result: 在WikiText-2和CC-News上，EmbPairSim的MRR比Sentence-BERT高达5.3 pp，且参数量显著更少。
conclusion: KG嵌入表示可作为紧凑有效的图级语义相似度信号，优于文本和结构方法。
---

## 摘要
知识图谱（KG）将事实表示为结构化三元组，广泛应用于跨领域的关系知识组织。正如文本信息从词汇、句子到完整文档，KG信息可以在多个层次上解读，从实体、关系、三元组到子图和整个KG。然而，现有KG嵌入方法主要关注实体、关系和三元组，图级语义在很大程度上未被处理。传统的图级方法通常基于结构模式比较图，但仅靠结构相似性无法保证KG之间的语义相似性。为了评估不同方法捕获此类图级语义信息的能力，我们研究图到图语义相似性，即判断一对KG是否表示语义对应的底层信息。为了获取可靠的真实对应关系，我们通过修改文本文档、从原始和修改后的文档中提取KG，并将已知对应关系迁移到KG对，构建了一个语义匹配数据集。我们在每个数据集上比较了基于文本、基于结构和基于KG嵌入的方法。对于基于KG嵌入的方法，我们引入了两种评分函数：EmbPairSim（使用最大成对实体相似性）和AvgEmbSim（使用频率加权质心）。在WikiText-2和CC-News上的实验表明，EmbPairSim在远少于参数量的情况下，MRR比Sentence-BERT高5.3个百分点。这些结果表明，KGE表示可以作为KG中图到图语义相似性的紧凑有效信号。我们的代码可在https://github.com/SeungRyeolBaek/KG-to-KG-Semantic-Similarity获取。

## Abstract
A Knowledge Graph (KG) represents facts as structured triples and is widely used to organize relational knowledge across diverse domains. Just as textual information ranges from words and sentences to complete documents, KG information can be interpreted at multiple levels, from entities, relations, and triples to subgraphs and entire KGs. However, existing KG embedding methods mainly focus on entities, relations, and triples, leaving graph-level semantics largely unaddressed. Conventional graph-level methods, which typically compare graphs based on structural patterns, are also insufficient because structural similarity alone cannot guarantee semantic similarity between KGs. To evaluate how well different methods capture such graph-level semantic information, we study graph-to-graph semantic similarity, which determines whether a pair of KGs represents semantically corresponding underlying information. To obtain reliable ground-truth correspondences, we construct a semantic matching dataset by modifying text documents, extracting KGs from both original and modified documents, and transferring their known correspondences to KG pairs. We compare text-based, structure-based, and KG embedding-based approaches on each dataset. For the KG embedding-based approach, we introduce two scoring functions: \textit{EmbPairSim}, which uses maximal pairwise entity similarity, and \textit{AvgEmbSim}, which uses a frequency-weighted centroid. Experiments on WikiText-2 and CC-News show that \textit{EmbPairSim} achieves up to 5.3 pp higher MRR than Sentence-BERT while using substantially fewer parameters. These results suggest that KGE representations can serve as compact and effective signals for graph-to-graph semantic similarity in KGs. Our code is available at https://github.com/SeungRyeolBaek/KG-to-KG-Semantic-Similarity.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：知识图谱（KG）的信息可以在多个层次解读（实体、关系、三元组、子图、整个KG），但现有KG嵌入方法主要聚焦于局部组件（实体、关系、三元组），忽略了图级语义信息。同时，传统基于结构的图级方法（如图核）仅依靠结构模式，不能保证语义对应。因此，如何评估KG之间的**图到图语义相似性**（判断两个KG是否表示相同底层信息）是一个未充分解决的问题。
- **动机**：需要可靠的评估框架来衡量不同方法捕获图级语义的能力。现有KG缺乏可控的真实语义对应关系，难以构建基准。
- **整体含义**：论文验证了KG嵌入（KGE）可以作为一种紧凑、有效的信号来度量图级语义相似性，为KG的层次化理解（类比文本从词到文档）提供了实证支持。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：利用已有的KG嵌入模型，通过聚合实体（和关系）嵌入来表征整个KG的语义，并设计两种评分函数计算图之间的相似性。同时，通过文本→KG的构造方式获取有监督的语义匹配数据。
- **关键技术细节**：
  - **语义匹配任务**：给定查询KG集合 G′ 和候选KG集合 G，对每个查询找到其真实对应的候选KG。
  - **数据集构建**：从文档（WikiText-2、CC-News）出发，通过不同强度的语义保留修改（同义词替换、上下文替换、DIPPER释义），将原始和修改后文档分别用LLM（GPT-3.5-turbo）提取为KG，从而获得已知语义对应的KG对。修改强度分6种设置。
  - **两种评分函数**：
    - **EmbPairSim**：计算两个KG所有实体嵌入之间的余弦相似度矩阵，对较小图中每个实体取最大相似度，统计大于阈值t的比例作为最终得分。支持均值中心化（提升对比度）。
    - **AvgEmbSim**：计算每个KG的频率加权平均实体嵌入（频率为该实体在三元组中出现次数），然后求两个平均向量的余弦相似度。计算效率更高。
  - **嵌入来源**：可使用转导式KGE（TransE、DistMult、ComplEx、RotatE，训练于所有参与比较的KG的联合集合）或归纳式KGE（INGRAM，预训练于NELL-995）。
  - **阈值选择**：通过网格搜索确定（RotatE: t=0.8; INGRAM: t=0.95）。

## 3. 实验设计：数据集、Benchmark、对比方法

- **数据集**：
  - WikiText-2（645篇维基百科文档）
  - CC-News（550篇新闻文章）
  - 每个数据集独立构造D、D′、G、G′，随机采样200篇文档生成修改版本。
- **Benchmark**：无现成的KG语义匹配基准，论文自行构造。
- **对比方法**：
  - **文本方法**：Sentence-BERT（all-mpnet-base-v2），将KG三元组口头化为OpenIE句子后编码；额外比较Word2Vec和FastText（训练/预训练两种设置）。
  - **结构方法**：顶点直方图核（VH）、Weisfeiler-Lehman核（WL）。
  - **KG嵌入方法**：TransE、DistMult、ComplEx、RotatE（转导式），INGRAM（归纳式）。嵌入维度32。
- **评价指标**：Hits@5、MRR、NDCG。

## 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量或训练时长。仅提及使用了GPT-3.5-turbo进行KG提取，以及KGE模型训练采用早停策略。文中指出“主要遵循原始论文的默认超参数”，但未给出算力细节。因此无法量化资源消耗。

## 5. 实验数量与充分性

- **实验组数量**：
  - 主实验（Table 1）：在2个数据集×3种修改类型×6种强度设置下，对比了9种方法（VH、WL、SBERT、EmbPairSim with RotatE/INGRAM、AvgEmbSim with RotatE/INGRAM），共2×6×9=108个场景。
  - 选择KGE模型实验（Figure 2）：在WikiText上比较4种KGE模型（TransE、DistMult、ComplEx、RotatE）×2种评分函数×6种修改，共48个数据点。
  - 消融实验（Table 2,3,4,5,6）：包括频率权重消融、均值中心化消融、关系嵌入消融、对比词嵌入、运行时效率。共5组独立实验。
- **充分性评价**：实验覆盖了多种修改强度（从同义词替换到复杂释义）、多种嵌入模型、多种基线（文本、结构、嵌入），并且进行了详细的消融分析。实验设计较为全面，对比公平（所有模型在相同数据上评估）。但仅用了两个数据集（WikiText-2和CC-News），领域覆盖有限（主要为英语百科和新闻）。未测试其他语言或专业领域KG。

## 6. 论文的主要结论与发现

1. **EmbPairSim整体最优**：在大多数设置下，EmbPairSim（使用RotatE或INGRAM）在Hits@5、MRR、NDCG上超越SBERT、图核和其他基线，最高MRR优势达5.3个百分点。
2. **KGE优于词嵌入**：同样使用EmbPairSim，KG嵌入（RotatE）的表现远好于Word2Vec和FastText词嵌入，表明KG嵌入更善于捕获图结构语义。
3. **结构信息足够**：归纳模型INGRAM（仅依赖结构上下文）与转导模型RotatE性能接近，说明KG的语义可通过局部结构有效表达。
4. **频率加权有益**：AvgEmbSim中采用频率加权平均通常优于简单平均（Table 2）。
5. **均值中心化对INGRAM至关重要**：INGRAM的嵌入分布集中，无均值中心化会导致EmbPairSim完全失效（Hits@5=0）。
6. **关系嵌入引入噪声**：添加关系嵌入会降低相似度性能（Table 4），可能因为关系信息已隐含在实体嵌入中。
7. **计算高效**：KGE方法（特别是AvgEmbSim）运行时远快于SBERT和图核（Table 6），适合大规模场景。

## 7. 优点

- **任务新颖**：首次构建了受控释义条件下的KG-to-KG语义检索基准，填补了图级语义相似性评估的空白。
- **轻量化方法**：EmbPairSim和AvgEmbSim无需重新训练，直接利用现成KGE，参数量远小于Sentence-BERT（如WikiText上EmbPairSim 779k vs. SBERT 495k，但实际EmbPairSim参数更少？注：表1中Emb size 779,008 < 495,360? 不，779k > 495k，但论文声称“far fewer parameters”可能指总参数或比较有一定问题，但总体KGE维度小、存储效率高）。
- **消融实验充分**：系统分析了频率权重、均值中心化、关系嵌入的影响，提供了可操作的工程指导。
- **可复现性**：代码开源，数据集构造流程清晰。

## 8. 不足与局限

- **数据集局限**：
  - 仅使用两个英语文本来源（维基百科、新闻），缺乏多语言、专业领域（生物、法律）的验证。
  - 文档规模有限（200篇），且KG提取依赖单一LLM（GPT-3.5-turbo），可能引入提取偏差。
- **方法局限**：
  - 简单聚合方法（如AvgEmbSim）在强释义（DIPPER）下性能明显下降，说明无法捕获全局拓扑变化。
  - 阈值t需要针对不同KGE模型单独网格搜索，缺乏自适应能力。
  - 未与端到端图神经网络（如GCN-based graph matching）比较。
- **实验设计不足**：
  - 未报告KGE训练的具体用时或硬件配置，算力透明度低。
  - 所有转导KGE模型统一训练于所有比较KG的联合图，但未测试在分离训练集下的泛化性（可能引入数据泄漏）。
  - 仅报告平均指标，未给出方差或统计显著性检验，无法评估随机性影响。
- **应用限制**：
  - 当前方法依赖已有KGE模型，不适用于新实体/关系频繁出现的场景（除非使用INGRAM）。
  - 对于大规模KG库的检索，AvgEmbSim效率虽高但精度远低于EmbPairSim；而EmbPairSim的成对计算复杂度为O(|E|·|E′|)，在实体数多时可能成为瓶颈。

（完）
