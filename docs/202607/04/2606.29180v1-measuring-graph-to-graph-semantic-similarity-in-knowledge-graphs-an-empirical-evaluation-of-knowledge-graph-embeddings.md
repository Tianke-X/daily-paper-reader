---
title: "Measuring Graph-to-Graph Semantic Similarity in Knowledge Graphs: An Empirical Evaluation of Knowledge Graph Embeddings"
title_zh: 知识图谱中图到图语义相似性的测量：知识图谱嵌入的实证评估
authors: "Seungryeol Baek, Wooseok Sim, Hogun Park"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29180v1"
tags: ["query:power-ticket"]
score: 9.0
evidence: 知识图谱嵌入用于图级语义相似度度量
tldr: 知识图谱广泛应用于组织关系知识，但图级语义相似性度量仍具挑战——现有结构或实体层面方法难以捕捉语义对应，传统图比较仅基于结构。本文通过修改WikiText-2和CC-News文本构建语义匹配数据集，全面对比文本、结构和KG嵌入方法，并提出基于KG嵌入的EmbPairSim和AvgEmbSim评分函数。实验表明EmbPairSim在MRR上超越Sentence-BERT最高5.3个百分点，参数效率显著。该工作证实KGE能作为紧凑有效的图级语义相似性信号，为跨KG语义匹配提供轻量方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29180v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 877, \"height\": 630, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.29180v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1846, \"height\": 554, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29180v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1815, \"height\": 1082, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29180v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 881, \"height\": 312, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29180v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 880, \"height\": 312, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29180v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 881, \"height\": 529, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29180v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 880, \"height\": 686, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.29180v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 484, \"height\": 276, \"label\": \"Table\"}]"
motivation: 现有KG嵌入主要关注实体关系，缺乏图级语义度量，传统结构比较无法反映语义对应，亟待有效图级语义相似性方法。
method: 通过修改文本构建KG语义匹配数据集，提出EmbPairSim和AvgEmbSim两种基于KGE的评分函数，分别利用最大实体相似度和频率加权质心。
result: 在WikiText-2和CC-News上，EmbPairSim的MRR比Sentence-BERT高最多5.3个百分点，参数大幅减少。
conclusion: KGE表示能作为紧凑有效的图级语义相似性信号，可用于跨KG信息匹配与融合。
---

## 摘要
知识图谱（KG）将事实表示为结构化三元组，广泛应用于组织不同领域的关联知识。正如文本信息从单词、句子到完整文档，KG信息可以在多个层次上解释，包括实体、关系、三元组、子图以及整个KG。然而，现有的KG嵌入方法主要关注实体、关系和三元组，图级别语义在很大程度上未被处理。传统的图级别方法通常基于结构模式比较图，也不足以满足需求，因为仅结构相似性不能保证KG之间的语义相似性。为了评估不同方法捕获这种图级别语义信息的效果，我们研究了图到图语义相似性，该相似性决定一对KG是否表示语义上对应的底层信息。为了获得可靠的真实对应关系，我们通过修改文本文档构建了一个语义匹配数据集，从原始和修改后的文档中提取KG，并将其已知对应关系转移到KG对。我们在每个数据集上比较了基于文本、基于结构和基于KG嵌入的方法。对于基于KG嵌入的方法，我们引入了两种评分函数：EmbPairSim（使用最大成对实体相似性）和AvgEmbSim（使用频率加权质心）。在WikiText-2和CC-News上的实验表明，EmbPairSim在参数数量显著减少的情况下，MRR比Sentence-BERT高出5.3个百分点。这些结果表明，KGE表示可以作为KG中图到图语义相似性的紧凑且有效的信号。我们的代码可在https://github.com/SeungRyeolBaek/KG-to-KG-Semantic-Similarity获取。

## Abstract
A Knowledge Graph (KG) represents facts as structured triples and is widely used to organize relational knowledge across diverse domains. Just as textual information ranges from words and sentences to complete documents, KG information can be interpreted at multiple levels, from entities, relations, and triples to subgraphs and entire KGs. However, existing KG embedding methods mainly focus on entities, relations, and triples, leaving graph-level semantics largely unaddressed. Conventional graph-level methods, which typically compare graphs based on structural patterns, are also insufficient because structural similarity alone cannot guarantee semantic similarity between KGs. To evaluate how well different methods capture such graph-level semantic information, we study graph-to-graph semantic similarity, which determines whether a pair of KGs represents semantically corresponding underlying information. To obtain reliable ground-truth correspondences, we construct a semantic matching dataset by modifying text documents, extracting KGs from both original and modified documents, and transferring their known correspondences to KG pairs. We compare text-based, structure-based, and KG embedding-based approaches on each dataset. For the KG embedding-based approach, we introduce two scoring functions: \textit{EmbPairSim}, which uses maximal pairwise entity similarity, and \textit{AvgEmbSim}, which uses a frequency-weighted centroid. Experiments on WikiText-2 and CC-News show that \textit{EmbPairSim} achieves up to 5.3 pp higher MRR than Sentence-BERT while using substantially fewer parameters. These results suggest that KGE representations can serve as compact and effective signals for graph-to-graph semantic similarity in KGs. Our code is available at https://github.com/SeungRyeolBaek/KG-to-KG-Semantic-Similarity.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：如何衡量两个知识图谱（KG）之间**图级别的语义相似性**，即判断两个KG是否表达了相同的底层信息（而非仅仅结构相似）。
- **背景**：现有KG嵌入方法（如TransE、ComplEx、RotatE等）主要关注实体、关系和三元组层面的表示，缺乏对**整个KG或子图**语义的建模。传统的图级别方法（如图核）只依赖结构特征（拓扑、标签分布），但结构相似不能保证语义对应。例如，同一事件的不同表述方式（同义词替换、句式变换）产生的KG结构可能差异很大，但语义相同。因此，需要一种能够捕捉图级语义相似性的评估框架。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 2.1 核心思想

- 通过**可控的文本修改**（同义词替换、上下文替换、DIPPER复述）生成语义等价的文档对，再提取为KG，从而获得已知语义对应的KG对。
- 利用**知识图谱嵌入（KGE）**的实体/关系向量，设计两种图级相似度评分函数，将KGE从局部组件提升到图级别。

### 2.2 关键技术细节

- **数据集构建流程**：
  1. 从WikiText-2和CC-News选取文档。
  2. 对部分文档进行修改（6种强度：同义词替换30%/60%，上下文替换30%/60%，DIPPER复述60/0和60/20）。
  3. 使用LLM（GPT-3.5-turbo + LangChain的LLMGraphTransformer）从原始和修改后的文档中提取KG。
  4. 形成查询KG集（修改版）和候选KG集（原始版+未修改文档），构成检索任务：每个查询KG需从候选集（含数百个干扰项）中找到其对应的原始KG。

- **提出的评分函数**（基于KGE）：
  - **EmbPairSim**：基于最大成对实体相似性。
    - 对两个KG的实体嵌入进行**均值中心化**（联合均值减去）。
    - 计算归一化后实体嵌入矩阵的点积，得到成余弦相似度矩阵。
    - 对较小KG中的每个实体，取其在较大KG中的最大相似度，统计超过阈值t的比例作为最终分数。
  - **AvgEmbSim**：频率加权质心相似性。
    - 对每个实体，按其出现频率（在KG三元组中出现的次数）加权平均实体嵌入，得到图级向量。
    - 计算两个图级向量的余弦相似度。

- **对比方法**：
  - 文本方法：Sentence-BERT（将三元组verbalize为句子后进行编码）、Word2Vec/FastText。
  - 结构方法：Vertex Histogram Kernel、Weisfeiler-Lehman Kernel。
  - 其他KGE方法：TransE、DistMult、ComplEx、RotatE（作为后端）。
  - 归纳式KGE：INGRAM（预训练模型，实体/关系完全未见）。

- **评估指标**：Hits@5, MRR, NDCG。

## 3. 实验设计：数据集、基准、对比方法

### 3.1 数据集

- **WikiText-2**：645篇Wikipedia文档（使用200篇作为查询，其余作为干扰项）。
- **CC-News**：550篇新闻文章（类似划分）。
- 每个数据集独立构建，均包含6种修改强度的设置。

### 3.2 基准（Baselines）

- **文本方法**：
  - Sentence-BERT（all-mpnet-base-v2）：参数422k~495k。
  - Word2Vec（Google News预训练/自训练）、FastText（自训练/预训练）。
- **结构方法**：Vertex Histogram Kernel、WL Kernel。
- **KGE方法**：
  - 传导式：TransE, DistMult, ComplEx, RotatE（嵌入维数32），训练在“所有对比图”的联合数据上。
  - 归纳式：INGRAM（预训练在NELL-995上，直接应用）。
- **提出的方法**：EmbPairSim + RotatE/INGRAM；AvgEmbSim + RotatE/INGRAM。

### 3.3 实验配置

- KG提取：使用GPT-3.5-turbo，默认prompt，无额外微调。
- 参数设置：KGE使用统一配置（维数32，margin=2，batch=128，lr=0.01，early stopping）。
- 阈值选择：EmbPairSim的阈值通过网格搜索确定（RotatE: 0.8, INGRAM: 0.95）。
- 设备与资源：未明确说明GPU型号/数量/训练时长，仅提到使用预训练模型和标准库。

## 4. 资源与算力

**文中未明确提及GPU型号、数量、训练时长等具体硬件信息。** 仅指出：
- 使用GPT-3.5-turbo API进行KG提取（需付费API调用）。
- KGE模型训练使用标准超参数（嵌入维数32，batch 128，LR 0.01），未说明训练时间或设备。
- Sentence-BERT、Word2Vec、FastText等均为预训练或小规模训练（Word2Vec/FastText在写作集上训练10 epoch）。
- 所有实验代码公开在GitHub，未提供算力细节。因此，无法量化算力消耗。

## 5. 实验数量与充分性

- **总实验量**：在2个数据集（WikiText-2和CC-News）× 6种修改设置（Synonym 30/60, Context 30/60, DIPPER 60/0, 60/20）× 多种方法（共约10+种），报告了三项指标（Hits@5, MRR, NDCG），表格数据稠密。
- **消融实验**：
  - 对AvgEmbSim比较了有无频率加权（Table 2）。
  - 对INGRAM的EmbPairSim比较了有无均值中心化（Table 3）。
  - 比较了实体嵌入 vs 实体+关系嵌入（Table 4）。
  - 比较了KGE vs 词嵌入（Word2Vec/FastText）用于EmbPairSim（Table 5）。
  - 运行时间比较（Table 6）。
- **充分性评价**：实验设计较为全面，覆盖了不同修改类型、多种语义表示方法、多个消融维度。缺点：缺少对更大数据集（如YAGO, Freebase）的验证；未对不同KGE模型（如TransE, DistMult, ComplEx）的效果差异进行深入分析（仅图2展示了NDCG，但主要用RotatE）；阈值选择在单一数据集上进行网格搜索，可能存在过拟合风险；所有实验依赖LLM提取KG，LLM的不确定性可能引入噪声。总体而言，在给定研究框架内实验充分且公平（固定超参，统一评估流程）。

## 6. 论文的主要结论与发现

1. **EmbPairSim在大多数设置下优于Sentence-BERT**，MRR最高提升5.3 pp，且参数数量少一个数量级。
2. **结构方法（图核）不如语义方法**：Vertex Histogram和WL Kernel表现低于KGE方法，说明仅靠结构标签无法捕捉语义对应。
3. **频率加权有助于AvgEmbSim**：加入实体频率（类似TF）平均提高效果（Table 2），表明实体重要性在KG中与词频类似。
4. **均值中心化对INGRAM至关重要**：无中心化时INGRAM的EmbPairSim完全失效（Hits@5=0），说明INGRAM嵌入空间存在全局偏移。
5. **关系嵌入引入噪声**：加入关系嵌入反而降低性能（Table 4），可能因为实体嵌入已隐式包含关系信息。
6. **KGE优于词嵌入**：RotatE/INGRAM比Word2Vec/FastText更适合KG语义比较（Table 5）。
7. **运行时高效**：EmbPairSim约0.38秒，AvgEmbSim约0.23秒，远快于SBERT（12秒）和图核（1.5-3.8秒）。

## 7. 优点

- **任务与数据集创新**：首次提出**基于文档修改的KG语义对应数据集**，提供了可控、可复现的测评基准。
- **方法简单有效**：EmbPairSim和AvgEmbSim无需额外训练，直接利用现成KGE，实现轻量级图级相似性度量。
- **全面的消融分析**：揭示了频率加权、均值中心化、关系嵌入、词嵌入等不同因素的影响，为后续工作提供洞察。
- **跨类型方法对比**：涵盖文本、结构、KGE三类代表方法，公平性好（统一参数、统一评估）。
- **代码开源**：促进可复现性。

## 8. 不足与局限

- **数据集规模有限**：仅使用两个中等规模文档集（WikiText-2和CC-News），且每个数据集只取200篇作为查询，干扰项数量有限（645或550）。未在大规模真实KG（如Wikidata、DBpedia）上验证。
- **KG提取依赖LLM**：使用GPT-3.5-turbo的默认流水线，未进行质量审核或消偏；不同LLM或提示词可能导致KG质量波动，影响结论泛化性。
- **阈值选择存在过拟合风险**：阈值（0.8和0.95）在特定数据集上网格搜索得到，可能不适用于其他场景。
- **未考虑子图级语义**：仅处理文档级KG（整个文档），未测试子图（如事件子图）的相似性。
- **仅评估检索任务**：未在其他任务（如分类、聚类）上验证图级嵌入的效果。
- **忽略多模态或跨语言场景**：论文聚焦英文文本，未涉及多语言KG或与其他模态（如图像）的结合。
- **算力信息缺失**：未提供训练/推理的硬件细节，不利于资源复现和效率对比。

（完）
