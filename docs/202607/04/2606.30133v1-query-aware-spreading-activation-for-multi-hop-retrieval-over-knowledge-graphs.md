---
title: Query-Aware Spreading Activation for Multi-Hop Retrieval over Knowledge Graphs
title_zh: 面向知识图谱多跳检索的查询感知传播激活方法
authors: "Illia Makarov, Mykola Glybovets"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30133v1"
tags: ["query:power-ticket"]
score: 9.0
evidence: 查询感知的图检索增强生成
tldr: 现有知识图谱增强检索在遍历时忽略查询语义，导致检索质量下降。本文提出查询感知传播激活方法，通过单步语义门控（候选实体描述与查询的余弦相似度）实现查询感知遍历，固定迭代次数，全程在Neo4j数据库内执行单一Cypher查询。在MuSiQue上EM达32.80，接近QAFD-RAG（33.50），并超越最强结构基线HippoRAG 5.3 EM和3.4 F1；消融实验显示门控带来3.6-7.4 F1提升并减少1.5-4.9倍延迟。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30133v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1796, \"height\": 378, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30133v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1770, \"height\": 950, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30133v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1821, \"height\": 748, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.30133v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1761, \"height\": 487, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30133v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 739, \"height\": 310, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30133v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1280, \"height\": 173, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30133v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 964, \"height\": 302, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30133v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1024, \"height\": 472, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30133v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1638, \"height\": 313, \"label\": \"Table\"}]"
motivation: 现有知识图谱增强检索的多跳遍历在种子节点后变为查询盲，依赖纯图结构，降低检索质量。
method: 提出查询感知传播激活，每步用余弦相似度门控候选实体描述与问题，固定迭代次数，在Neo4j中单次Cypher查询完成全检索。
result: 在MuSiQue上EM 32.80，接近QAFD-RAG的33.50，超越HippoRAG 5.3 EM和3.4 F1；门控带来3.6-7.4 F1提升并降低1.5-4.9倍延迟。
conclusion: 查询感知门控在保持检索质量的同时简化流程，减少延迟，便于与图数据库集成。
---

## 摘要
基于知识图谱的检索增强生成（Graph RAG）通过利用图结构，在多跳问答中优于平面段落检索。然而，在大多数现有系统中，问题仅设定种子节点；后续遍历变得“查询盲化”，完全依赖图结构。例外是QAFD-RAG，它通过结合边重新加权的流扩散求解器实现查询感知遍历。该架构需要将完整图加载到Python内存中，并采用迭代次数可变的求解器，这复杂化了与图数据库的集成。我们提出了一种传播激活方法，通过单个逐步骤语义门实现相同的查询感知遍历：步骤权重是候选实体描述与问题之间的余弦相似度，且迭代次数固定。整个检索流程——种子映射、传播、Top-K选择及上下文组装——表示为一条Cypher查询，在与Neo4j的一次往返中执行；图永不离开数据库。在MuSiQue上，我们的方法在精确匹配上媲美QAFD-RAG（32.80 vs 33.50），并在EM（精确匹配）上领先最强纯结构基线HippoRAG 5.3个百分点，在F1上领先3.4个百分点；在2WikiMultiHopQA上，HippoRAG和QAFD-RAG因其短语节点架构保持优势。一项禁用该门的消融实验证实，该门是F1同时提升3.6至7.4个百分点、检索延迟降低1.5至4.9倍的来源。

## Abstract
Retrieval-augmented generation built on knowledge graphs (Graph RAG) outperforms flat passage retrieval on multi-hop question answering by leveraging graph structure. In most existing systems, however, the question only sets the seed nodes; the subsequent traversal becomes "query-blind", depending solely on the graph structure. The exception is QAFD-RAG, which implements query-aware traversal via a flow-diffusion solver with combined edge re-weighting. This architecture requires loading the full graph into Python memory and an iterative solver with a variable number of iterations complicating integration with the graph database. We propose a spreading-activation method that achieves the same query-aware traversal with a single per-step semantic gate: the step weight is the cosine similarity between the candidate entity's description and the question, and the number of iterations is fixed. The whole retrieval procedure - seed mapping, propagation, top-K selection and context assembly - is expressed as a single Cypher query executed in one round-trip to Neo4j; the graph never leaves the database. On MuSiQue our method matches QAFD-RAG by exact match (32.80 vs 33.50) and outperforms the strongest purely-structural baseline in our comparison, HippoRAG, by 5.3 EM and 3.4 F1; on 2WikiMultiHopQA HippoRAG and QAFD-RAG retain an advantage due to their phrase-node architectures. An ablation with the gate disabled confirms that the gate is the source of a simultaneous F1 gain of 3.6 to 7.4 points and a retrieval-latency reduction by a factor of 1.5 to 4.9.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 核心问题与整体含义（研究动机和背景）

- **问题**：现有基于知识图谱的检索增强生成（Graph RAG）系统在处理多跳问答时，问题仅用于选取种子节点，之后的图遍历完全依赖图结构（如PageRank、社区检测），即“查询盲”（query-blind）。这导致相关信息可能因结构路径长或表面词汇不重叠而丢失。
- **例外与不足**：QAFD-RAG首次实现查询感知遍历（通过流扩散+边重加权），但需要将整个图加载到Python内存中，且迭代次数不固定（依赖收敛条件），难以集成到图数据库（如Neo4j）中。
- **本文目标**：提出一种**查询感知的传播激活方法**，用单步语义门（余弦相似度）替代复杂边重加权，固定迭代次数，整个检索过程用一条Cypher查询在Neo4j内完成，无需将图移出数据库。同时提升准确率并降低延迟。

#### 2. 方法论：核心思想、关键技术细节

- **核心思想**：基于**传播激活**（spreading activation）机制，从种子节点开始，每步只向与问题语义相关的邻居传播激活。
- **关键技术细节**：
  - **索引阶段**：将语料分文档，用LLM（Gemini 2.5 Flash）提取实体（类型为PERSON/ORGANIZATION等）及其描述，以及关系三元组。通过名称子串匹配+LLM进行实体消歧，最后将实体、关系写入Neo4j，并用text-embedding-004生成768维描述向量，建立向量索引。
  - **检索阶段（算法1）**：
    1. **种子映射**：从问题中提取实体（LLM结构化调用），通过精确名称匹配或向量回退（余弦相似度）找到图节点，初始化激活值（归一化到[0,1]）。
    2. **传播迭代（T=3，固定）**：
       - 每步对每个候选实体v，计算语义门 \( \sigma(v) = \max(\cos(e_v, e_q), 0) \)。
       - 接收来自活跃邻居u的激活增量：\( \Delta r^{(t)}(v) = \alpha \cdot \sigma(v) \cdot \sum_{u \in A^{(t)}(v)} r^{(t-1)}(u) \)，其中\( A^{(t)}(v) \) 要求邻居激活值大于阈值τ且大于v当前激活（单调递减条件，避免循环）。
       - 仅当\( \Delta r^{(t)}(v) > \tau \)时才更新。
    3. **链与实体选择**：选取激活最高的Ke个实体及其描述，以及Kc条从种子到活跃节点的路径（最长4跳，仅经过被传播访问的节点），按平均激活排序。路径去重（反转、前缀吸收）。
    4. **上下文组装**：将路径和实体描述拼接，送入LLM生成答案。
  - **Cypher实现**：整个流程封装为一条Cypher查询，使用`resource_map`（Cypher MAP）维护激活状态，通过`CALL subquery`实现传播迭代，无需客户端与数据库之间交换数据。

#### 3. 实验设计

- **数据集**：
  - MuSiQue：由2-4个单跳问题组合而成，共1000题（标准样本），另用500题做消融。
  - 2WikiMultiHopQA：基于Wikipedia结构化信息，包括桥接和比较问题，共1000题（标准样本），消融用500题。
- **对比方法**：
  - GraphRAG (2024), LightRAG (2025), HippoRAG (2024), PathRAG (2025), QAFD-RAG (2026)。
  - 对比数据来自QAFD-RAG论文中作者对基线的复现结果（同一实验协议，但使用GPT-4o-mini）。
- **评估指标**：token-level F1和Exact Match (EM)，标准预处理（小写、去停用词等）。
- **基础设施**：Neo4j 5.26 Community + GDS/APOC插件，本地Docker（4GB heap，2GB page cache），MacBook Pro M1 Pro (10核, 16GB RAM)。LLM统一使用Gemini 2.5 Flash（温度0），嵌入模型Google text-embedding-004。

#### 4. 资源与算力

- **未明确给出GPU型号、数量、训练时长**。论文使用API调用LLM（Gemini 2.5 Flash）进行实体提取、消歧、问答生成，不涉及模型训练。
- **计算资源**：单台MacBook Pro M1 Pro（无独显），Neo4j运行在Docker中。检索阶段延迟以秒计（表VI）。索引阶段需要大量LLM调用（约1万文档），但未报告具体耗时和API成本。
- **作者声明**：由于API预算限制，仅使用Gemini 2.5 Flash，未用GPT-4o-mini复现对比系统。

#### 5. 实验数量与充分性

- **实验组数**：
  - 主要对比：2个数据集×5个方法（本文+4个基线），n=1000。
  - 消融实验：11种配置（naive-vector、no-query-aware、T=1~4、α=0.3~1.0），n=500，覆盖门控、深度、衰减因子。
  - 延迟与token成本测量：5种配置，n=500。
- **充分性分析**：
  - 消融实验系统，证实了查询感知门控（同时提升F1和减少延迟）和深度T=2的关键性。
  - 不足之处：
    - 主要对比中基线数值来自其他论文复现，使用不同LLM（GPT-4o-mini vs Gemini 2.5 Flash），样本重叠未知，公平性存疑。
    - 仅使用单一LLM，未评估对提取器或生成器变化的敏感性。
    - 消融实验样本量n=500，置信区间约±0.04 F1，部分配置差异在区间内，但总体趋势明确。
  - 总体客观，但对比条件不完全对等。

#### 6. 主要结论与发现

- **核心发现**：查询感知门控**同时提升准确率和降低延迟**。在MuSiQue上，EM 32.80几乎追上QAFD-RAG的33.50，F1 41.68（领先HippoRAG 3.4点），EM领先HippoRAG 5.3点。在2Wiki上F1 55.00，落后HippoRAG约15点（因架构差异见局限）。
- **门控消融**：禁用门控后F1下降3.6-7.4点，延迟增加1.5-4.9倍（因前向膨胀），证明门控通过剪枝低信息分支同时提升速度和准确性。
- **深度敏感度**：T=1->2带来最大F1提升（+18.8在2Wiki，+12.7在MuSiQue），T=2后趋于饱和。
- **衰减因子α**：在0.3-1.0范围内F1波动小（≤4点），无需精细调参。

#### 7. 优点

- **方法简洁高效**：用单步余弦门替代复杂边重加权，固定迭代次数，无需内求收敛条件，易于理解和实现。
- **数据库原生集成**：整个检索流程用单条Cypher查询在Neo4j内完成，图数据永不离开数据库，便于生产部署、并行化、并发安全、备份与容错。
- **同时优化准确率和速度**：门控剪枝低相关性分支，减少后续计算量，同时让Top-K更聚焦，优于无门控统一传播。
- **在硬数据穆西克上匹配最强方法**：MuSiQue上的EM与QAFD-RAG几乎持平，且超过所有纯结构基线，验证了查询感知的必要性。

#### 8. 不足与局限

- **在2WikiMultiHopQA上显著落后**（F1差距约15点）。原因：
  - 实体类型受限（5类，不含日期、国籍等属性），导致属性答案无法作为独立节点检索。
  - 比较型问题（如“谁更早出生”）不需多跳链式推理，图遍历优势有限。
  - HippoRAG等使用OpenIE短语节点，能直接访问原子事实。
- **对比实验公平性存疑**：基线结果来自QAFD-RAG论文复现，使用GPT-4o-mini而非Gemini 2.5 Flash，样本重叠未知，可能引入系统偏差。
- **消融实验规模有限**：仅n=500，置信区间较宽，部分ablation配置差异可能不显著。
- **未测试对LLM提取器的敏感性**：实体提取和问答生成使用同一Gemini模型，若换成其他模型（如GPT-4o）可能影响门控判别力。
- **索引阶段依赖LLM**：实体/关系提取和消歧都调用LLM，成本高，且可能漏提或产生幻觉。

（完）
