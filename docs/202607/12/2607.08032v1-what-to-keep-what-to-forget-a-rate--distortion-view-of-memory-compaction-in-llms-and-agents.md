---
title: "What to Keep, What to Forget: A Rate--Distortion View of Memory Compaction in LLMs and Agents"
title_zh: 保留与遗忘：大语言模型与智能体中内存压缩的率失真视角
authors: "Ashwin Gerard Colaco, Nada Lahjouji"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08032v1"
tags: ["query:llm-memory"]
score: 9.0
evidence: LLM与agent中内存压缩的统一视角
tldr: 大模型和智能体中的KV缓存、提示、状态及记忆压缩可统一视为率失真问题。本文提出统一压缩目标和层无关下界，构建七轴分类法，揭示各层压缩均依赖注意力幅度或近因信号，且在未知查询前丢弃信息导致失败。当前评估仅关注单轮长上下文，缺乏对智能体迭代压缩的衡量。据此提出基准建议和设计原则。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08032v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1230, \"height\": 473, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08032v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 800, \"height\": 464, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08032v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1682, \"height\": 194, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08032v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 712, \"height\": 395, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08032v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 818, \"height\": 617, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08032v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 807, \"height\": 614, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08032v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1523, \"height\": 484, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08032v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1694, \"height\": 608, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08032v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 941, \"height\": 549, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08032v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 721, \"height\": 620, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08032v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 852, \"height\": 609, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08032v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1093, \"height\": 1177, \"label\": \"Table\"}]"
motivation: 大模型和智能体的记忆消耗日益增长，各研究社区独立压缩但缺少统一理论，导致方法碎片化。
method: 从率失真理论出发，提出单一压缩目标和层无关下界，构建七轴分类法，统一分析KV缓存、提示、状态和记忆压缩。
result: 发现各层压缩决策信号均为注意力幅度或近因，在未知查询前丢弃信息；当前评估仅测单轮长上下文，忽略智能体迭代压缩。
conclusion: 提出压缩感知设计原则和跨层基准建议，通过参考实验验证，并列出开放问题。
---

## 摘要
大语言模型及其构建的智能体在记忆方面耗费的计算和内存越来越多：缓存注意力键和值、携带长提示、维护循环状态、存储先前轮次和会话中的事件。由于这些记忆并非免费，四个大致独立的研究社区各自学会了压缩它们。它们驱逐或量化KV缓存、修剪或蒸馏提示、约束架构状态、并整合智能体记忆。我们认为这些都是同一个问题的实例：一个关于在资源预算下，以何种保真度保留或丢弃从上下文派生出的信息的率失真决策，以保持下游任务效用。我们用一个单一的压缩目标和层无关下界精确化这个视角，用它构建一个七轴分类法，统一分类来自整个栈的方法，并用它在从未连接过的层之间转移机制，从服务栈的KV管理到智能体长期记忆。整个综述中有两个模式。在每个层，决定保留什么的信号是注意力幅度或新近度，并且它在所有地方以相同方式失败：在查询未知且无法撤销的情况下，丢弃了查询后来需要的信息。尽管压缩在单轮长上下文上被仔细衡量，但智能体实际执行的重复压缩几乎从未被测量，也没有基准一次性在所有层上持有一个预算轴。我们将这两个观察转化为一个基准提案、一个小型参考实验和一组压缩感知设计原则，并绘制了开放问题。

## Abstract
Large language models, and the agents built on them, spend an ever-growing share of their compute and memory on remembering: caching attention keys and values, carrying long prompts, maintaining recurrent state, and storing what happened in previous turns and sessions. Because none of this memory is free, four largely separate research communities have each learned to compact it. They evict or quantize the KV cache, prune or distill prompts, bound architectural state, and consolidate agent memory. We argue that these are instances of one problem: a rate--distortion decision about what context-derived information to retain versus discard, at what fidelity, under a resource budget, so as to preserve downstream task utility. We make this lens precise with a single compaction objective and a layer-agnostic lower bound, use it to build a seven-axis taxonomy that classifies methods from across the stack uniformly, and use it to transfer mechanisms between layers that have never been connected, from serving-stack KV management to agent long-term memory. Two patterns hold across the survey. At every layer the signal that decides what to keep is attention magnitude or recency, and it fails in the same way everywhere, by discarding, before the query is known and with no way to undo it, information the query later needs. And while compression is measured carefully on single-turn long context, the repeated compaction that agents actually perform is almost never measured, and no benchmark holds one budget axis across all the layers at once. We turn both observations into a benchmark proposal, a small reference experiment, and a set of compaction-aware design principles, and we map the open problems.

---

## 论文详细总结（自动生成）

好的，这是根据您提供的论文内容生成的详细中文总结。

### 论文中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLMs）和基于其构建的智能体（Agents）在处理长文本和长时间任务时，会消耗大量计算和内存资源用于“记忆”。这些记忆形式多样，包括KV缓存（Key-Value cache）、长提示（prompts）、循环状态（recurrent state）以及历史交互记录。
- **研究动机**：目前，针对这些不同记忆形式的压缩方法分属四个独立的研究社区（KV缓存压缩、提示/上下文压缩、架构状态压缩、智能体语义记忆压缩），它们各自使用独立的基准和评估指标，方法之间缺乏交流。作者认为，这本质上都是同一个问题：在给定的资源预算下，决定保留或丢弃哪些上下文信息，以最小的性能损失（失真）换取最大的内存节省（率）。
- **整体含义**：本文旨在通过引入**率失真理论（Rate-Distortion Theory）** 和**信息瓶颈（Information Bottleneck）** 原则，为这些看似分散的压缩问题提供一个**统一的、理论驱动的分析框架**。通过该框架，文章揭示了不同层面压缩方法的共性、核心失败模式，并提出了跨层迁移机制和统一评估基准的可能性。

#### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将记忆压缩问题形式化为一个**率失真优化问题**。
    - **统一目标**：给定历史信息 `H`，找到一个紧凑表示 `Z`，使其大小 `rate(Z)` 满足预算 `B`，同时最小化下游任务失真 `D`。
    - **关键公式**：作者推导出一个**层无关的下界**，说明任何形式的记忆压缩（无论是KV缓存、提示、状态还是智能体记忆）都无法逃脱：
        - 当预算 `B` 低于任务所需的信息量 `I*(Q)` 时，错误概率 `P_e` 必然上升，且受同一表达式约束（Fano不等式）。
        - 与查询无关的压缩（query-agnostic）比与查询相关的压缩（query-conditioned）需要支付额外的“代价”，即查询的熵 `H(Q)`。
        - 这个下界统一了四个看似不同的压缩领域，解释了为什么信息量高的任务（如多跳检索）难压缩，而信息量低的任务（如总结）易压缩。

- **关键技术细节**：
    1.  **七轴分类法（Seven-Axis Taxonomy）**：为了统一描述所有压缩方法，提出一个维度体系，包括：压缩单元粒度、作用生命周期、有损性与保真度、查询/任务适应性、可学习性、实现机制、存储介质。
    2.  **三个核心属性**：用于解释方法成败的关键：
        - **可逆性（Reversibility）**：丢弃的信息能否在后续被重新获取或恢复。
        - **查询条件性（Query-conditioning）**：压缩决策是否依赖或知晓未来的查询。
        - **保真度剖面（Fidelity profile）**：表示是无损、近无损、均匀有损还是多保真度（如准确+摘要）。
    3.  **跨层桥梁**：论文建立了一个“推断-智能体记忆”桥梁，将不同层面的设计旋钮（如重要性信号、遗忘窗口、查询条件性、可逆性、停止规则）对应起来，并展示了如何将一个层面（如KV缓存）的技术（如Quest的无驱逐检索、Ada-KV的输出误差边界）迁移到另一个层面（如智能体长期记忆）。

- **算法流程**：论文的“方法”更多是提供一个分析框架，而非提出一个具体算法。其流程通常是：
    - 定义一个压缩算子 `C` 和 使用算子 `U`。
    - 寻找一个**失真替代量（distortion surrogate）**，如注意力分数、困惑度、互信息估计等，来代理未知的真实任务损失。
    - 根据预算 `B`，使用该替代表达式来决定保留或丢弃信息。

#### 3. 实验设计：数据集、Benchmark 与对比方法

- **实验场景与数据集**：
    - **单轮统一预算前沿实验**：使用“大海捞针”（Needle-in-a-Haystack）任务，但使用自然文本（Wikitext）作为填充物。任务是在不同深度和长度（2k到8k tokens）的上下文中，回忆一个特定的键值对。
    - **多轮误差累积实验**：模拟智能体场景，让模型分块阅读长文档，累积12个关键事实，并在多次压缩后最终回忆所有事实。

- **提出的 Benchmark（COMPACT-Bench）**：论文本身没有运行全面的基准测试，而是提出了一个设计蓝图，包括：
    - **统一预算轴**：所有方法都映射到“**每历史令牌字节数（Bytes-Per-Token, BPT）**”指标上。
    - **三任务族**：除了最终准确率，还需测量**损失归因**（能否知道丢了什么）、**可逆性**（能否恢复）、**校准的压缩置信度**（模型是否知道自己不自信）。
    - **成本报告**：同时报告准确率、延迟、内存和成本。

- **对比方法**：
    - **实验1**：对比了6种KV缓存压缩方法：**SnapKV**, **StreamingLLM**, **TOVA**，以及**Knorm**和**期望注意力分数**评分器，和一个**随机驱逐**控制组。全缓存作为上限基准。
    - **实验2**：对比了两种代理记忆操作：
        - **不可逆操作**：使用LLM生成的摘要覆盖工作记忆。
        - **可逆操作**：将所有信息归档，在查询时检索相关部分。

#### 4. 资源与算力

- **使用的资源**：
    - 单块 **NVIDIA RTX 4060 GPU**，显存为8GB。
    - 模型为 **Qwen2.5-1.5B-Instruct**。
- **算力说明**：作者明确表示这是一个小规模的参考实验，意在展示方法论而非争夺SOTA。实验结果（如具体的准确率数值）与被模型规模绑定，但观察到的**趋势**（如预算低于信息需求时准确率崩溃、可逆与不可逆记忆在多轮下的分离）是可复现的。未提及训练时长和更多GPU数量。

#### 5. 实验数量与充分性

- **实验数量**：
    - **实验1**：包含1,395次生成（~1400次），在5种预算水平、多种深度和长度下进行，每个设置3次试验。实验量级中等。
    - **实验2**：模拟了多次压缩事件（5到25次）下的表现。实验量级较小。
- **充分性与客观性**：
    - **优点**：实验设计巧妙，能够有力地凸显其理论框架的核心预测：（1）统一预算轴下的准确率崩溃现象；（2）多轮压缩下可逆与不可逆操作的性能分离。这证明了其分析框架的实用性。
    - **不足**：实验规模有限（小模型、单GPU、单一任务类型），无法全面衡量所有压缩方法。实验主要作为论点的**示范**，而非严格的、大规模的基准测试。作者本身也承认其局限性，将其定位为“参考实验”，并呼吁更大规模的复现，因此不具有完备代表性。

#### 6. 论文的主要结论与发现

- **统一视角有效**：四个独立社区的记忆压缩问题本质上是同一个**率失真决策**，可以被统一的形式化框架所描述。
- **统一的失败模式**：在所有层面上，压缩决策的核心信号（注意力、新近度）都**在查询未知且无法撤销的情况下**做出，导致系统性地丢弃了后续查询所需的关键信息。这是预测1的具体体现。
- **关键属性决定成败**：在相同预算下，**可逆的**（能找回）和**查询相关的**（决策前能看到查询）操作**无可争议地优于**不可逆的和查询无关的操作。参考实验证明了这一点，尤其是在多轮交互中，可逆与不可逆记忆的差距会急剧扩大。
- **评估差距巨大**：现有基准只评估**单轮长上下文**压缩，完全忽略了**智能体实际面临的重复压缩**场景。智能体迭代压缩下的误差累积效应未被衡量。
- **提出Agenda**：论文提出了一个包含**9个开放问题**的优先级研究议程，核心是让压缩操作变得**查询相关、可逆和可归因**。

#### 7. 优点

- **思想深度与统一性**：首次将率失真理论成功应用于整个LLM和智能体记忆压缩堆栈，提供了强大的、理论驱动的统一视角，结束了该领域的碎片化状态。
- **理论严谨**：推导出了适用于所有压缩层的**层无关信息论下界**，使问题形式化，并能解释不同方法的根本差异和共同局限。
- **跨层迁移的洞察力**：提出了“推断-智能体记忆”桥梁，揭示了不同层面设计是同一问题的实例，并展示了跨层机制迁移（如将KV层的技术用于智能体记忆）的可能性，具有前瞻性。
- **基准设计的前瞻性**：提出的**COMPACT-Bench**理念（统一预算轴、三属性评估、多轮压缩）直击当前评估体系的痛点，为未来建立更公正、更全面的评估标准指明了方向。
- **开放问题清晰**：文章对后续工作的指导性强。识别出的9个开放问题（如“操作符代数”、“信息损失归因”）对于推动领域发展具有重要意义。

#### 8. 不足与局限

- **实验验证规模小**：论文本身只是理论上构建了框架并进行了小规模示意性实验，**缺乏大规模、跨多模型、多任务的全面实证验证**。其核心结论（如下界、可逆性重要性）在更大规模设置下的表现尚需验证。
- **理论假设的简化**：下界推导中假设了查询无关压缩的马尔可夫链 `Y-H-Z` 等理想情况。实践中，任务条件信息量 `I*(Q)` 的精确估计非常困难，该公式目前更多是定性或概念性的指导，而非量化的计算工具。
- **跨层“操作符代数”缺失**：虽然论文提出了“操作符代数”作为开放问题，认为KV缓存的不同压缩技术（量化、驱逐等）的叠加应该被研究，但论文本身并未提供此类实证结果。
- **参考实验的局限性**：基准实验是在单个小GPU和小规模模型上完成的，且任务仅局限于简单的“大海捞针”和事实回忆。模型和任务的局限性限制了结论的普适性。论文明确指出了这一点。
- **未涉及安全与可审计性**：论文在开放问题中提到了记忆的安全、可审计和可回滚问题（如后门攻击、毒素注入），但正文对此的讨论相对较少且较为概括，更多是将其作为未来工作的一部分。

（完）
