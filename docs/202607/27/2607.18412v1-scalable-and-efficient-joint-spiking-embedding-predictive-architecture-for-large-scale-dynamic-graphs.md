---
title: Scalable and Efficient Joint Spiking Embedding Predictive Architecture for Large-Scale Dynamic Graphs
title_zh: 面向大规模动态图的可扩展高效联合脉冲嵌入预测架构
authors: "Huizhe Zhang, Yuchang Zhu, Huazhen Zhong, Liang Chen, Zibin Zheng"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18412v1"
tags: ["query:power-ticket"]
score: 9.0
evidence: 动态图结构学习
tldr: 动态图学习面临标注数据稀缺和计算开销大的挑战，现有自监督方法依赖复杂的边级重建或图增强，难以扩展到大图。本文提出SG-JEPA，一种联合尖峰嵌入预测架构，通过时空上下文预测和尖峰神经元编码实现高效学习。实验表明，SG-JEPA在节点分类上达到竞争性性能，并有效处理1300万条边的动态图，避免了负采样、图增强等机制，显著提升训练效率和内存可扩展性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18412v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1652, \"height\": 739, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18412v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1480, \"height\": 584, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18412v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 863, \"height\": 467, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18412v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 862, \"height\": 427, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18412v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 872, \"height\": 860, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18412v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 882, \"height\": 219, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18412v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1520, \"height\": 626, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18412v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1509, \"height\": 625, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18412v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 876, \"height\": 483, \"label\": \"Table\"}]"
motivation: 现有自监督动态图方法依赖复杂的目标（如边级重建）和昂贵的图增强，阻碍了向大规模图扩展。
method: 提出SG-JEPA，沿时间维度划分节点为上下文和目标集，利用尖峰神经元编码粗到细的脉冲计数嵌入实现预测式自监督学习。
result: SG-JEPA在节点分类上达到或超越判别式基线，可扩展到1300万条边的动态图，训练效率和内存优于先前自监督基线。
conclusion: SG-JEPA通过避免复杂机制（负采样、图增强等）实现了高效可扩展的动态图自监督学习。
---

## 摘要
动态图学习旨在捕捉现实世界系统（如欺诈检测和推荐系统）中不断变化的结构和语义模式。由于真实动态图中标注数据的稀缺性，近年研究引入了生成式或对比式范式（例如掩码图自编码器或图对比学习）来生成与任务无关的图嵌入。然而，这些方法通常依赖于复杂的边级重建目标和定制的图增强策略，在扩展到大规模动态图时会带来巨大的计算开销。本文提出SG-JEPA，一种面向大规模动态图的联合脉冲嵌入预测架构。与现有自监督方法不同，SG-JEPA沿着时间维度将节点划分为上下文集和目标集，通过额外的时空信息学习彼此可预测的嵌入。此外，通过将序列输入编码为从粗到细的脉冲计数嵌入，脉冲神经元使SG-JEPA能够适应下游任务不同的计算约束。大量实验表明，SG-JEPA在节点分类上达到了与判别式基线相当甚至更优的性能，并能有效扩展到具有1300万条边的动态图。SG-JEPA避免了复杂的机制（负采样、图增强、边级重建等），相比于先前的自监督动态图基线，在训练效率和内存可扩展性方面表现更优。

## Abstract
Dynamic graph learning aims to capture evolving structural and semantic patterns in real-world systems, such as fraud detection and recommender systems. Due to the scarcity of labeled data in real-world dynamic graphs, recent studies have introduced generative or contrastive paradigms (e.g., masked graph autoencoders or graph contrastive learning) to generate task-agnostic graph embeddings. However, these methods typically rely on complex edge-level reconstruction objectives and tailored graph augmentation strategies. This incurs substantial computational overhead when scaling to large-scale dynamic graphs. In this paper, we propose SG-JEPA, a joint spiking embedding predictive architecture for large-scale dynamic graphs. In contrast to existing self-supervised methods, SG-JEPA partitions nodes into context and target sets along the temporal dimension to learn embeddings that are predictive of each other via additional spatial-temporal information. Furthermore, through encoding sequential inputs into coarse-to-fine spike count embeddings, spiking neurons enable SG-JEPA to adapt to the varying computational constraints of downstream tasks. Extensive experiments demonstrate that SG-JEPA achieves competitive or even superior performance over discriminative baselines on node classification, while effectively scaling to the dynamic graph with 13 million edges. SG-JEPA avoids the complex machinery (negative sampling, graph augmentations, edge-level reconstruction, etc.), resulting in superior training efficiency and memory scalability compared with prior self-supervised dynamic graph baselines.

---

## 论文详细总结（自动生成）

# 论文详细总结：SG-JEPA

## 1. 核心问题与整体含义（研究动机与背景）
- **问题**：现实中的动态图（如社交网络、推荐系统）数据稀疏且标注成本高，而现有的自监督动态图学习方法（对比学习、掩码自编码器）依赖于复杂的边级重建或精心设计的图增强策略，这导致：
  - 计算开销大，难以扩展到大规模图（如千万级边）；
  - 预训练目标与下游任务存在偏差；
  - 需要手动选择增强策略，适应性差。
- **动机**：联合嵌入预测架构（JEPA）在计算机视觉中已展现出无需重建或对比即可学习语义表示的能力，但其在动态图领域的应用尚未被探索。作者认为 JEPA 的预测学习原则天然适合动态图的时序演化。
- **总体目标**：提出一种高效、可扩展的脉冲驱动联合嵌入预测架构（SG-JEPA），在不依赖负采样、图增强或边重建的前提下，实现大规模动态图的无监督表示学习。

## 2. 方法论：核心思想与关键技术
### 核心思想
- **联合嵌入预测**：将动态图的时序演化建模为预测问题——通过历史时空上下文预测未来节点表示，且预测直接在嵌入空间进行，避免重建原始输入。
- **粗到细的脉冲计数嵌入**：利用脉冲神经元（PLIF）将序列全精度嵌入转换为离散脉冲序列，并通过累加得到多分辨率上下文表示，实现计算效率与表达能力之间的自适应平衡。

### 关键技术细节（5个模块，对应图2）
1. **时间划分（Temporal Partitioning）**  
   - 将快照序列划分为非重叠时间窗口（窗口大小 \( w \)）。
   - 每个窗口中，最后快照的节点作为**目标节点**，前面 \( w-1 \) 个快照的节点作为**上下文节点**。
   - 通过固定大小邻居采样保持计算成本可控。

2. **目标编码（Target Encoding）**  
   - 对每个节点融合：原始特征 \( x \)、位置编码（随机游走PE）、时间编码（正弦TE）。
   - 使用图神经网络（如GraphSAGE）对每个快照生成全精度节点嵌入 \( z_{v,t} \)。
   - 窗口最后一步的嵌入 \( z_{v,iw} \) 作为**目标表示**。

3. **上下文编码（Context Encoding）**  
   - 将上下文节点在不同快照的全精度嵌入送入脉冲神经网络（PLIF神经元）生成离散脉冲 \( s_{v,t} \in \{0,1\}^d \)。
   - 脉冲序列按时间累加得到**脉冲计数嵌入** \( c_{v,t} = \sum_{t' \le t} s_{v,t'} \)。
   - 形成粗到细的分层表示：早期步骤只积累少量脉冲（粗），晚期步骤积累更多脉冲（细）。

4. **目标表示预测（Target Representation Prediction）**  
   - 将不同精度的上下文脉冲计数嵌入拼接，通过可学习投影矩阵 \( W \) 映射到统一空间。
   - 引入窗口级可学习令牌 \( m_t \) 捕获全局时序动态。
   - 使用可学习加权平均与共享MLP生成预测嵌入 \( \hat{z}_{v,w} \)。

5. **损失函数**  
   - 采用InfoNCE对比损失，使预测嵌入与目标嵌入相似，同时与窗口中其他目标节点嵌入区分。
   - 使用stop-gradient防止表示坍塌。

### 公式要点（文字说明）
- 膜电位更新与脉冲发放：使用PLIF神经元（含可学习膜时间常数）。
- 损失函数：InfoNCE，温度超参数 \( \tau \)，相似度函数 \( \text{sim} \)。

## 3. 实验设计
### 数据集（表1）
| 数据集 | 节点数 | 边数 | 类别数 | 时间步 | 规模 |
|--------|--------|------|--------|--------|------|
| DBLP   | 28,085 | 236,894 | 10 | 27 | 小 |
| Tmall  | 577,314 | 4,807,545 | 5 | 186 | 中 |
| Patent | 2,738,012 | 13,960,811 | 6 | 25 | 大（1300万边） |

### 对比方法
- **全精度监督/半监督DGNN**：JODIE、EvolveGCN、TGAT、ROLAND
- **自监督DGNN**：CLDG（对比）、MaskDGNN（生成式）
- **脉冲DGNN**：GC-SNN、SpikeNet、Dy-SIGN、Delay-DSGN、SiGNN

### 评估指标
- Macro-F1、Micro-F1 (分类任务)
- 三种训练比例：40%、60%、80%

## 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、数量、总训练时长。
- 仅在图3中给出了每个epoch的训练时间对比（秒级），但未提供具体硬件配置。
- 提及训练效率时使用了“per-epoch time”和“最大内存使用”等指标，但缺乏绝对算力信息。

## 5. 实验数量与充分性
- **主要实验**：在3个数据集上、3种训练比例下进行节点分类，报告Macro-F1和Micro-F1（共18组结果），重复5次取平均。
- **训练效率分析**：对比每epoch时间、内存使用，以及收敛曲线（图4）。
- **参数敏感性**：对窗口大小、GNN层数、邻居采样数、膜电位阈值进行调参（图5）。
- **消融实验**（表4）：
  - 替换脉冲神经元（IF、LIF）
  - 改变目标编码更新策略（去掉stop-grad、使用EMA）
  - 替换损失函数（L1、L2、余弦）
- **公平性**：所有基线通过超参数搜索获得最优配置，实验重复5次并报告方差。对比方法涵盖不同类型（全精度、自监督、脉冲），覆盖不同类型方法。
- **充分性评价**：实验设计较为全面，不仅验证了性能，还分析了效率和可扩展性。但缺少链接预测任务（论文主要聚焦节点分类），且未在十亿级边数据集上验证。

## 6. 主要结论与发现
- **性能**：SG-JEPA在节点分类上达到与最先进脉冲监督方法（SiGNN）几乎相同的性能（差距仅0.085%），且**显著优于所有自监督基线**。
- **效率**：每epoch训练时间比自监督基线平均快5.2倍（DBLP）和2.8倍（Tmall），内存使用仅高于CLDG，远低于MaskDGNN等生成式方法。
- **可扩展性**：成功处理1300万条边的Patent数据集，而对比方法ROLAND和MaskDGNN出现OOM。
- **稳定性**：预测损失与下游性能同步下降，无表示坍塌或任务不匹配问题。

## 7. 优点
1. **首次将JEPA引入动态图**：开辟了新的自监督范式，避免了复杂增强和重建。
2. **脉冲驱动的粗到细编码**：通过脉冲计数嵌入实现多分辨率表示，可自适应下游任务计算约束（少上下文快照仍有效）。
3. **高度可扩展**：固定邻居采样 + 脉冲稀疏计算，内存和训练时间随图大小线性增长。
4. **设计简约**：无需负采样、图增强、边解码器，减少超参数调优负担。
5. **消融实验揭示了关键设计**：如stop-grad、InfoNCE损失、PLIF神经元不可替代。

## 8. 不足与局限
1. **任务覆盖不全**：仅评估节点分类，未在链接预测、图分类等任务上验证。
2. **缺少极端大规模测试**：最大数据集为1300万边，未在十亿级甚至更大图上实验。
3. **未提供算力细节**：无法复现训练成本，影响工程可重现性。
4. **窗口大小需手动设定**：虽然敏感性分析表明一定鲁棒性，但最优窗口仍依赖数据集。
5. **脉冲网络理论效率未实践验证**：虽然声称能量效率高，但未进行实际功耗或推理速度对比。
6. **对超大规模节点可能仍有瓶颈**：全监督基线（ROLAND）在Patent上OOM，SG-JEPA虽然成功但未分析极限。

（完）
